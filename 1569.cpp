#include <bits/stdc++.h>
using namespace std;

const int MXN = 100, MXM = 1e5;
int n, m;
vector<pair<int, int>> adj;

void solve() {
  int<pair<int, int>> needed;
  double minDiameter = MXN;
  // center is at vertex
  for
  // center is at edge
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cin >> n >> m;
  int u, v;
  for (int i = 0; i < m; ++i) {
    cin >> u >> v;
    adj.push_back(make_pair(u, v));
  }
  solve();
}